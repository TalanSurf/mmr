# Build output (regenerated by CMake / rebuild-inject.ps1)
/build/

# Runtime data — per-user, not part of the project
*.log
config.cfg

# Xenos injector: NOT required. inject.ps1 is a self-contained PowerShell
# injector, so these are excluded to keep the repo lean.
Xenos64.exe
XenosCurrentProfile.xpr

# The prebuilt momentum_menu.dll IS committed on purpose so users can run the
# tool without building it. (It's the static, standalone x64 build.)
